# Committed to git on purpose — this is a fully-offline demo project with no secrets.
# Override in .env.local for non-default values.

DATABASE_URL="file:./dev.db"

# iron-session needs >= 32 chars; this is a dev-only default.
SESSION_PASSWORD="veda-dental-demo-dev-session-password-change-me-in-production"
