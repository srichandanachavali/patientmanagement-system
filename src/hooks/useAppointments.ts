// ── F041 · src/hooks/useAppointments.ts
// Purpose: Client hook stub — fetches appointments filtered by date / dentist_id / chair_id (Phase D)
// In: filter params | Out: { data, isLoading, error } | See: F023, F070
export {}
