// ── F191 · src/components/shared/SkeletonLoader.tsx
// Purpose: Animated loading skeleton — pulse opacity 0.5→1, 1.2s loop for data-heavy page states (stub — Phase U1)
// In: — | Out: SkeletonLoader | See: —
export function SkeletonLoader() { return null }
