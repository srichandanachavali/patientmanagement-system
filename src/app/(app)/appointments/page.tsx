// ── F150 · src/app/(app)/appointments/page.tsx
// Purpose: Appointments calendar page — renders DayView client component
// In: DayView (F153) | Out: AppointmentsPage | See: F153
import { DayView } from '@/components/scheduler/DayView'

export default function AppointmentsPage() {
  return <DayView />
}
